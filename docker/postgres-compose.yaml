services:
  unit_postgres:
    image: postgres:18.1
    container_name: unit_postgres
    restart: always
    env_file:
      - postgres.env
    ports:
      - '5432:5432'
    volumes: 
      - /mnt/folderpath:/var/lib/postgresql